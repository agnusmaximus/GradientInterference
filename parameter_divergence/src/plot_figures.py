import sys

def extract_parameter_differences(text):
    for line in text.splitlines():
        if line != "":

def plot_figures(f):
    f_file = open(f, "r")
    rawtext = f_file.read()
    f_file.close()

    extract_parameter_differences(raw_text)

if __name__=="__main__":
    inputs = []
    for f in inputs:
        plot_figures(f)
